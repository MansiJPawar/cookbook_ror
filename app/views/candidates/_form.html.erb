
<%= form_with(model: candidate) do |form| %>
  <% if candidate.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(candidate.errors.count, "error") %> prohibited this candidate from being saved:</h2>

      <ul>
        <% candidate.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :profile, style: "display: block" %>
    <%= form.text_field :profile %>
  </div>

  <div>
    <%= form.label :letter, style: "display: block" %>
    <%= form.text_area :letter %>
  </div>

  <div>
    <%= form.label :immediate, style: "display: block" %>
    <%= form.check_box :immediate %>
  </div>

  <div>
    <%= form.submit%>
  </div>
<% end %>
